spring.application.name=demo-tls-rest

server.port=8443

# enable/disable https
server.ssl.enabled=true
# keystore format
server.ssl.key-store-type=PKCS12
# keystore location
server.ssl.key-store=classpath:certs/keystore.p12
# keystore password
server.ssl.key-store-password=changeit

